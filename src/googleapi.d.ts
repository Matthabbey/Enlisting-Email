declare module 'googleapi' {
  export interface Options {
    // define the properties that the options object should have
  }

  export class GoogleApi {
    // declare the methods and properties of the GoogleApi class
  }
}
